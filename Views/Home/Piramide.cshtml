@model int
@{
        int ball=Model-1;
        int ss=0;
        int ff=ball;
}

    @for (int i = 0 ; i < Model ; i++)
    {
        @for (int d = 0;d < ball-ss ; d++){
            <img  src="~/images/cuadro.png" alt="#">
        }
        ss++;
        @for (int k=0; k < Model-ff ; k++){
         <img  src="~/images/bloque.webp" alt="#">
    }
    ff--;
    <div></div>
    }
    <br/>